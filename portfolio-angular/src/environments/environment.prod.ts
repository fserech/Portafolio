// src/environments/environment.prod.ts  (producción)
export const environment = {
  production: true,
  // Cuando despliegues en producción, pon la URL real de tu servidor:
  apiUrl: 'https://tu-servidor.com:3000'
  // Por ejemplo con Railway, Render, VPS, etc.
};
